language: go
sudo: required
before_install:
- export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcloud-service-key.json" && export APPENGINE_DEV_APPSERVER="${HOME}/google-cloud-sdk/bin/dev_appserver.py" && source "${HOME}/google-cloud-sdk/path.bash.inc" && go test -v $TRAVIS_BUILD_DIR/... 
- $TRAVIS_BUILD_DIR/annotator.sh
go:
- 1.6
- 1.8
